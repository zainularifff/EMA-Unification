import { useCallback, useEffect, useMemo, useRef, useState, type ReactNode } from 'react';
import { Ban, CheckCircle2, ChevronDown, ChevronRight, Database, Folder, FolderOpen, Laptop, Layers, Loader2, Package, RefreshCw, RotateCcw, Save, ShieldAlert, ShieldCheck } from 'lucide-react';
import restrictionService, { getCurrentLoginId, type RestrictionPackage, type RestrictionPolicyDetail, type RestrictionPolicyRow, type RestrictionStatusRow, type RestrictionTarget, type RestrictionTreeNode, type WhitelistSoftware } from '../services/restrictionService';
import { EmaButton, EmaFilterField, EmaKpiCard, EmaKpiGrid, EmaPageLayout, EmaPagination, EmaSearchInput, EmaSection, EmaSidebarPanel, EmaSidebarTreeRow, EmaTable, EmaTableShell, EmaToastViewport, EmaToolbar, type EmaTableColumn, type EmaToastItem, type EmaToastTone } from '../components/ema';

type AppModule = 'appBlacklist' | 'appWhitelist';
type SubTab = 'status' | 'settings' | 'policyStatus';
type PackageView = 'all' | 'selected' | 'available';
type LookupRow = RestrictionPackage | WhitelistSoftware;

type FormState = {
  policyId: number;
  inheritPolicy: boolean;
  exception: boolean;
  updateInterval: string;
  weeklyPolicy: boolean;
  schedule1: string;
  schedule2: string;
  schedule3: string;
  schedule4: string;
  appRestrictType: '1' | '2' | '3';
  appNoticeMessage: string;
  processRestrictType: '0' | '1' | '2' | '3';
  processNoticeMessage: string;
  fontRestrictType: '0' | '1' | '2' | '3';
  fontNoticeMessage: string;
};

const MODULES = [
  { id: 'appBlacklist' as const, label: 'App Restriction', shortLabel: 'Restriction', helper: 'Block unauthorized software and executable usage.', icon: ShieldAlert, tone: 'rose' as const },
  { id: 'appWhitelist' as const, label: 'App Whitelist', shortLabel: 'Whitelist', helper: 'Permit approved software and application execution.', icon: ShieldCheck, tone: 'emerald' as const },
];
const TAB_LABELS: Record<SubTab, string> = { status: 'Restriction Status', settings: 'Policy Settings', policyStatus: 'Policy Status' };
const DAY_OPTIONS = ['MON', 'TUE', 'WED', 'THU', 'FRI', 'SAT', 'SUN'];
const PAGE_SIZE = 10;
const LOOKUP_PAGE_SIZE = 12;
const DEFAULT_FORM: FormState = { policyId: 0, inheritPolicy: false, exception: false, updateInterval: '120', weeklyPolicy: false, schedule1: '', schedule2: '', schedule3: '', schedule4: '', appRestrictType: '1', appNoticeMessage: '', processRestrictType: '0', processNoticeMessage: '', fontRestrictType: '0', fontNoticeMessage: '' };

function cn(...values: Array<string | false | null | undefined>) { return values.filter(Boolean).join(' '); }
function today() { return new Date().toISOString().slice(0, 10); }
function daysAgo(days: number) { const date = new Date(); date.setDate(date.getDate() - days); return date.toISOString().slice(0, 10); }
function asArray<T = any>(value: unknown): T[] { if (Array.isArray(value)) return value as T[]; if (value && typeof value === 'object') { const row = value as Record<string, unknown>; if (Array.isArray(row.data)) return row.data as T[]; if (Array.isArray(row.rows)) return row.rows as T[]; if (Array.isArray(row.recordset)) return row.recordset as T[]; } return []; }
function cleanText(value: unknown, fallback = '') { const text = value === undefined || value === null ? '' : String(value).trim(); return text || fallback; }
function keyify(value: string) { return value.toLowerCase().replace(/[\s_\-().]/g, ''); }
function pick(row: Record<string, unknown> | null | undefined, keys: string[], fallback = '-') { if (!row) return fallback; const map = new Map(Object.entries(row).map(([key, value]) => [keyify(key), value])); for (const key of keys) { const direct = row[key]; const mapped = map.get(keyify(key)); const value = direct !== undefined ? direct : mapped; if (value !== undefined && value !== null && String(value).trim() !== '') return String(value); } return fallback; }
function pickNumber(row: Record<string, unknown> | null | undefined, keys: string[]) { const parsed = Number(pick(row, keys, '').replace(/,/g, '')); return Number.isFinite(parsed) ? parsed : 0; }
function getPackageId(item: Partial<LookupRow> | null | undefined) { return cleanText((item as any)?.SW_Pkg_Idn || (item as any)?.SW_Pkg_IDN || (item as any)?.sw_pkg_idn || (item as any)?.PackageID || (item as any)?.packageId || (item as any)?.WLSWIdn || (item as any)?.WLSWIDN || (item as any)?.id || (item as any)?.ID); }
function getPackageName(item: Partial<LookupRow> | null | undefined) { return cleanText((item as any)?.SW_Pkg_Name || (item as any)?.SW_Pkg_NAME || (item as any)?.PackageName || (item as any)?.packageName || (item as any)?.Name || (item as any)?.name || (item as any)?.SW_Name || (item as any)?.softwareName, 'Unnamed package'); }
function getNodeLabel(node: RestrictionTreeNode) { return cleanText(node.label || node.name || node.Object_Rel_Name || node.Object_Full_Name || node.ComputerName || node.Object_DeviceID || node.MDM_DeviceID, 'Unnamed scope'); }
function getNodeId(node: RestrictionTreeNode, index = 0) { return cleanText(node.id || node.ID || node.Object_Rel_Idn || node.Object_Root_Idn || node.target_id || node.Object_DeviceID || node.MDM_DeviceID, `node-${index}`); }
function getChildren(node: RestrictionTreeNode) { return asArray<RestrictionTreeNode>(node.children || node.Children || node.items || node.nodes); }
function getNodeType(node: RestrictionTreeNode): 'root' | 'department' | 'device' { const type = String(node.type || node.Type || '').toLowerCase(); if (type.includes('device') || node.Object_Root_Idn || node.Object_DeviceID || node.MDM_DeviceID) return 'device'; if (type.includes('root') || type.includes('org')) return 'root'; return 'department'; }
function getTarget(node: RestrictionTreeNode, index = 0): RestrictionTarget { const type = getNodeType(node); const label = getNodeLabel(node); if (type === 'root') return { id: 'root', label: 'All Branches', type: 'root', target_type: 1, target_id: '-1', Object_Full_Name: 'Root Policy' }; if (type === 'device') { const rootId = cleanText(node.Object_Root_Idn || node.objectRootIdn || node.MDM_Asset_Idn || node.assetId || node.target_id || node.id || index); return { id: `device-${rootId}`, label, type: 'device', target_type: 3, target_id: rootId, Object_Root_Idn: rootId, Object_DeviceID: node.Object_DeviceID || node.MDM_DeviceID || node.DeviceID || '', Object_Full_Name: node.Object_Full_Name || node.Department || node.Branch || '' }; } const relationId = cleanText(node.Object_Rel_Idn || node.objectRelIdn || node.target_id || node.id || index); return { id: `department-${relationId}`, label, type: 'department', target_type: 2, target_id: relationId, Object_Rel_Idn: relationId, Object_Full_Name: node.Object_Full_Name || node.path || label }; }
function countNodes(node: RestrictionTreeNode): number { const count = pickNumber(node, ['count', 'badge', 'total', 'deviceCount', 'TotalDevices']); if (count > 0) return count; if (getNodeType(node) === 'device') return 1; return getChildren(node).reduce((total, child) => total + countNodes(child), 0); }
function filterTree(nodes: RestrictionTreeNode[], query: string): RestrictionTreeNode[] { const search = query.trim().toLowerCase(); if (!search) return nodes; return nodes.map((node) => { const children = filterTree(getChildren(node), search); const label = [getNodeLabel(node), node.Object_Full_Name, node.Object_DeviceID, node.MDM_DeviceID].filter(Boolean).join(' ').toLowerCase(); return label.includes(search) || children.length ? { ...node, children } : null; }).filter(Boolean) as RestrictionTreeNode[]; }
function findFirstTarget(nodes: RestrictionTreeNode[]): RestrictionTarget | null { for (let index = 0; index < nodes.length; index += 1) { const node = nodes[index]; const target = getTarget(node, index); if (target) return target; const nested = findFirstTarget(getChildren(node)); if (nested) return nested; } return null; }
function getSetting(policy: RestrictionPolicyDetail | null, key: string, fallback = '') { if (!policy) return fallback; const direct = policy.settings?.[key] ?? policy[key]; if (direct !== undefined && direct !== null && String(direct).trim() !== '') return String(direct); const found = asArray<any>(policy.settingItems || policy.items || policy.settingsList).find((item) => cleanText(item.policy_key || item.key).toLowerCase() === key.toLowerCase()); return found?.policy_value !== undefined && found?.policy_value !== null ? String(found.policy_value) : fallback; }
function settingValues(policy: RestrictionPolicyDetail | null, key: string) { return asArray<any>(policy?.settingItems || policy?.items || policy?.settingsList).filter((item) => cleanText(item.policy_key || item.key).toLowerCase() === key.toLowerCase()).sort((a, b) => Number(a.seq || 0) - Number(b.seq || 0)).map((item) => cleanText(item.policy_value || item.value)).filter(Boolean); }
function splitDays(value: string) { const upper = value.toUpperCase(); if (!upper) return []; if (upper.includes(',')) return upper.split(',').map((item) => item.trim()).filter(Boolean); return DAY_OPTIONS.filter((day) => upper.includes(day)); }
function formFromPolicy(module: AppModule, policy: RestrictionPolicyDetail | null): FormState { const schedules = settingValues(policy, 'SoftwareRestrictSchedule'); return { policyId: Number(policy?.policy_id || policy?.PolicyID || policy?.id || 0), inheritPolicy: getSetting(policy, 'parent_policy', '0') !== '0' || getSetting(policy, 'use_parent_policy', '0') === '1', exception: getSetting(policy, 'use_policy', '1') === '0', updateInterval: getSetting(policy, module === 'appWhitelist' ? 'update_log_interval' : 'update_policy_result_interval', '120'), weeklyPolicy: getSetting(policy, 'use_weekly_policy', '0') === '1', schedule1: schedules[0] || '', schedule2: schedules[1] || '', schedule3: schedules[2] || '', schedule4: schedules[3] || '', appRestrictType: (getSetting(policy, 'SoftwareRestrictType', '1') as FormState['appRestrictType']) || '1', appNoticeMessage: getSetting(policy, 'SoftwareRestrictMessage', ''), processRestrictType: (getSetting(policy, 'process_restrict_type', '0') as FormState['processRestrictType']) || '0', processNoticeMessage: getSetting(policy, 'process_restrict_message', ''), fontRestrictType: (getSetting(policy, 'font_restrict_type', '0') as FormState['fontRestrictType']) || '0', fontNoticeMessage: getSetting(policy, 'font_restrict_message', '') }; }
function selectedPolicyIds(policy: RestrictionPolicyDetail | null, sourceRows: LookupRow[]) { if (!policy) return []; const direct = [...asArray<any>(policy.package_list), ...asArray<any>(policy.packages), ...asArray<any>(policy.selectedPackages), ...asArray<any>(policy.whitelistSoftware)]; const directIds = direct.map((item) => (typeof item === 'object' ? getPackageId(item) : cleanText(item))).filter(Boolean); if (directIds.length) return Array.from(new Set(directIds)); const values = asArray<any>(policy.settingItems || policy.items || policy.settingsList).filter((item) => /package|software|whitelist/i.test(cleanText(item.policy_key || item.key))).map((item) => cleanText(item.policy_value || item.value)).filter(Boolean); if (values.length) return Array.from(new Set(values)); return Array.from(new Set(sourceRows.filter((row: any) => row.Selected || row.selected || row.use_policy).map(getPackageId).filter(Boolean))); }
function badgeClass(value: string) { const text = value.toLowerCase(); if (text.includes('blocked') || text.includes('denied') || text.includes('restrict')) return 'border-rose-200 bg-rose-50 text-rose-700'; if (text.includes('allowed') || text.includes('success') || text.includes('installed')) return 'border-emerald-200 bg-emerald-50 text-emerald-700'; if (text.includes('pending') || text.includes('waiting')) return 'border-amber-200 bg-amber-50 text-amber-700'; return 'border-slate-200 bg-slate-50 text-slate-700'; }
function Badge({ children, className }: { children: ReactNode; className: string }) { return <span className={cn('inline-flex rounded-full border px-3 py-1 text-xs font-black', className)}>{children}</span>; }
function FieldLabel({ children }: { children: ReactNode }) { return <label className="mb-2 block text-[11px] font-black uppercase tracking-[0.16em] text-slate-500">{children}</label>; }
function pageSlice<T>(rows: T[], page: number, size: number) { const totalPages = Math.max(1, Math.ceil(rows.length / size)); const safePage = Math.min(Math.max(1, page), totalPages); const start = (safePage - 1) * size; return { totalPages, safePage, start, rows: rows.slice(start, start + size) }; }

export default function AppRestriction() {
  const [activeModule, setActiveModule] = useState<AppModule>('appBlacklist');
  const [activeTab, setActiveTab] = useState<SubTab>('status');
  const [treeNodes, setTreeNodes] = useState<RestrictionTreeNode[]>([]);
  const [expandedNodes, setExpandedNodes] = useState<Set<string>>(new Set(['root', 'organization', 'all']));
  const [selectedTarget, setSelectedTarget] = useState<RestrictionTarget | null>(null);
  const [treeSearch, setTreeSearch] = useState('');
  const [statusSearch, setStatusSearch] = useState('');
  const [policySearch, setPolicySearch] = useState('');
  const [packageSearch, setPackageSearch] = useState('');
  const [packagePage, setPackagePage] = useState(1);
  const [statusPage, setStatusPage] = useState(1);
  const [policyPage, setPolicyPage] = useState(1);
  const [packageView, setPackageView] = useState<PackageView>('all');
  const [startDate, setStartDate] = useState(daysAgo(30));
  const [endDate, setEndDate] = useState(today());
  const [includeSub, setIncludeSub] = useState(true);
  const [loadingTree, setLoadingTree] = useState(false);
  const [loadingData, setLoadingData] = useState(false);
  const [saving, setSaving] = useState(false);
  const [policyRows, setPolicyRows] = useState<RestrictionPolicyRow[]>([]);
  const [statusRows, setStatusRows] = useState<RestrictionStatusRow[]>([]);
  const [packages, setPackages] = useState<RestrictionPackage[]>([]);
  const [whitelistSoftware, setWhitelistSoftware] = useState<WhitelistSoftware[]>([]);
  const [form, setForm] = useState<FormState>(DEFAULT_FORM);
  const [selectedPackageIds, setSelectedPackageIds] = useState<string[]>([]);
  const [selectedWhitelistIds, setSelectedWhitelistIds] = useState<string[]>([]);
  const [selectedDays, setSelectedDays] = useState<string[]>([]);
  const [toasts, setToasts] = useState<EmaToastItem[]>([]);
  const toastIdRef = useRef(0);

  const moduleConfig = MODULES.find((item) => item.id === activeModule) || MODULES[0];
  const isWhitelist = activeModule === 'appWhitelist';
  const filteredTree = useMemo(() => filterTree(treeNodes, treeSearch), [treeNodes, treeSearch]);
  const lookupRows = isWhitelist ? whitelistSoftware : packages;
  const selectedIds = useMemo(() => Array.from(new Set((isWhitelist ? selectedWhitelistIds : selectedPackageIds).filter(Boolean))), [isWhitelist, selectedPackageIds, selectedWhitelistIds]);
  const selectedSet = useMemo(() => new Set(selectedIds), [selectedIds]);
  const selectedLookupCount = useMemo(() => lookupRows.reduce((total, row) => total + (selectedSet.has(getPackageId(row)) ? 1 : 0), 0), [lookupRows, selectedSet]);
  const availableLookupCount = Math.max(lookupRows.length - selectedLookupCount, 0);
  const filteredLookupRows = useMemo(() => { const term = packageSearch.trim().toLowerCase(); return lookupRows.filter((row) => { const id = getPackageId(row); const selected = selectedSet.has(id); if (packageView === 'selected' && !selected) return false; if (packageView === 'available' && selected) return false; const text = [getPackageName(row), id, pick(row, ['SW_Pkg_Company', 'Company', 'vendor'], ''), pick(row, ['SW_Catg', 'category', 'Category'], '')].join(' ').toLowerCase(); return !term || text.includes(term); }); }, [lookupRows, packageSearch, packageView, selectedSet]);
  const filteredStatusRows = useMemo(() => { const term = statusSearch.trim().toLowerCase(); return statusRows.filter((row) => !term || Object.values(row || {}).join(' ').toLowerCase().includes(term)); }, [statusRows, statusSearch]);
  const filteredPolicyRows = useMemo(() => { const term = policySearch.trim().toLowerCase(); return policyRows.filter((row) => !term || Object.values(row || {}).join(' ').toLowerCase().includes(term)); }, [policyRows, policySearch]);
  const statusPaged = pageSlice(filteredStatusRows, statusPage, PAGE_SIZE);
  const policyPaged = pageSlice(filteredPolicyRows, policyPage, PAGE_SIZE);
  const lookupPaged = pageSlice(filteredLookupRows, packagePage, LOOKUP_PAGE_SIZE);

  const addToast = useCallback((tone: EmaToastTone, title: string, message?: string) => { toastIdRef.current += 1; const toast: EmaToastItem = { id: `${Date.now()}-${toastIdRef.current}`, tone, title, message }; setToasts((items) => [...items.slice(-2), toast]); window.setTimeout(() => setToasts((items) => items.filter((item) => item.id !== toast.id)), 3200); }, []);
  const loadTree = useCallback(async () => { setLoadingTree(true); try { const nodes = await restrictionService.getTree(); const next = Array.isArray(nodes) ? nodes : []; setTreeNodes(next); setSelectedTarget((current) => current || findFirstTarget(next)); setExpandedNodes((current) => { const merged = new Set(current); next.slice(0, 2).forEach((node, index) => merged.add(getNodeId(node, index))); return merged; }); } catch (error) { addToast('error', 'Unable to load branch scope', error instanceof Error ? error.message : 'Please check restriction API.'); } finally { setLoadingTree(false); } }, [addToast]);
  const loadLookups = useCallback(async () => { const [packageRows, whitelistRows] = await Promise.all([restrictionService.getPackages().catch(() => []), restrictionService.getWhitelistSoftware().catch(() => [])]); const nextPackages = Array.isArray(packageRows) ? packageRows : []; const nextWhitelist = Array.isArray(whitelistRows) ? whitelistRows : []; setPackages(nextPackages); setWhitelistSoftware(nextWhitelist); return { packages: nextPackages, whitelistSoftware: nextWhitelist }; }, []);
  const loadPolicyData = useCallback(async () => { if (!selectedTarget) return; setLoadingData(true); try { const [lookups, effectivePolicy, policies, statuses] = await Promise.all([loadLookups(), restrictionService.getEffectivePolicy(activeModule, selectedTarget), restrictionService.getPolicyList(activeModule, selectedTarget).catch(() => []), restrictionService.getRestrictionStatus(activeModule, selectedTarget, { search: statusSearch || undefined, startDate, endDate, includeSub: includeSub ? 1 : 0 }).catch(() => [])]); const policy = (effectivePolicy || {}) as RestrictionPolicyDetail; const source = activeModule === 'appWhitelist' ? lookups.whitelistSoftware : lookups.packages; setForm(formFromPolicy(activeModule, policy)); setSelectedDays(splitDays(getSetting(policy, 'day_select', ''))); setPolicyRows(Array.isArray(policies) ? policies : []); setStatusRows(Array.isArray(statuses) ? statuses : []); const selected = selectedPolicyIds(policy, source); if (activeModule === 'appWhitelist') setSelectedWhitelistIds(selected); else setSelectedPackageIds(selected); } catch (error) { addToast('error', 'Unable to load policy', error instanceof Error ? error.message : 'Please check restriction API.'); } finally { setLoadingData(false); } }, [activeModule, addToast, endDate, includeSub, loadLookups, selectedTarget, startDate, statusSearch]);

  useEffect(() => { void loadTree(); }, [loadTree]);
  useEffect(() => { void loadPolicyData(); }, [loadPolicyData]);
  useEffect(() => { setStatusSearch(''); setPolicySearch(''); setPackageSearch(''); setPackageView('all'); setPackagePage(1); setStatusPage(1); setPolicyPage(1); }, [activeModule]);
  useEffect(() => { setPackagePage(1); }, [packageSearch, packageView, activeModule, filteredLookupRows.length]);
  useEffect(() => { setStatusPage(1); }, [statusSearch, filteredStatusRows.length]);
  useEffect(() => { setPolicyPage(1); }, [policySearch, filteredPolicyRows.length]);

  const updateForm = (key: keyof FormState, value: any) => setForm((current) => ({ ...current, [key]: value }));
  const toggleDay = (day: string) => setSelectedDays((current) => (current.includes(day) ? current.filter((item) => item !== day) : [...current, day]));
  const togglePackage = (id: string) => { if (!id) return; const updater = (current: string[]) => { const next = new Set(current.filter(Boolean)); if (next.has(id)) next.delete(id); else next.add(id); return Array.from(next); }; if (isWhitelist) setSelectedWhitelistIds(updater); else setSelectedPackageIds(updater); };
  const savePolicy = async () => { if (!selectedTarget) { addToast('warning', 'Select target first', 'Choose a branch or device before saving policy.'); return; } setSaving(true); try { const basePayload = { policy_id: form.policyId || 0, target_type: selectedTarget.target_type ?? selectedTarget.targetType ?? 1, target_id: selectedTarget.target_id ?? selectedTarget.targetId ?? selectedTarget.id ?? '-1', Object_Rel_Idn: selectedTarget.Object_Rel_Idn, Object_Root_Idn: selectedTarget.Object_Root_Idn, Object_DeviceID: selectedTarget.Object_DeviceID, use_parent_policy: isWhitelist ? '0' : form.inheritPolicy ? '1' : '0', use_policy: form.exception ? '0' : '1', update_interval: form.updateInterval || '120', use_weekly_policy: form.weeklyPolicy ? '1' : '0', day_select: selectedDays.join(','), use_schedule: '0', login_id: getCurrentLoginId(), console_ip: '' }; await restrictionService.savePolicy(activeModule, activeModule === 'appBlacklist' ? { ...basePayload, restrict_type: form.appRestrictType, restrict_message: form.appNoticeMessage, version_compare: '0', softwareRestrictSchedule1: form.schedule1, softwareRestrictSchedule2: form.schedule2, softwareRestrictSchedule3: form.schedule3, softwareRestrictSchedule4: form.schedule4, package_list: selectedIds } : { ...basePayload, restrict_type: form.processRestrictType, restrict_message: form.processNoticeMessage, font_restrict_type: form.fontRestrictType, font_restrict_message: form.fontNoticeMessage, package_list: selectedIds }); addToast('success', 'Policy saved', `${moduleConfig.label} saved successfully.`); await loadPolicyData(); } catch (error) { addToast('error', 'Policy save failed', error instanceof Error ? error.message : 'Please check restriction API.'); } finally { setSaving(false); } };

  function renderTree(nodes: RestrictionTreeNode[], depth = 0): ReactNode { return <div>{nodes.map((node, index) => { const id = getNodeId(node, index); const label = getNodeLabel(node); const children = getChildren(node); const open = expandedNodes.has(id); const target = getTarget(node, index); const active = selectedTarget?.id === target.id; const type = getNodeType(node); const Icon = type === 'device' ? Laptop : children.length && open ? FolderOpen : Folder; return <div key={id}><EmaSidebarTreeRow active={active} depth={depth} onClick={() => { setSelectedTarget(target); if (children.length) setExpandedNodes((current) => new Set(current).add(id)); }}><button type="button" onClick={(event) => { event.stopPropagation(); setExpandedNodes((current) => { const next = new Set(current); if (next.has(id)) next.delete(id); else next.add(id); return next; }); }} className="grid size-5 shrink-0 place-items-center text-slate-500">{children.length ? (open ? <ChevronDown size={14} /> : <ChevronRight size={14} />) : null}</button>{type === 'root' ? <Layers size={16} /> : <Icon size={16} />}<span className="min-w-0 flex-1 truncate">{type === 'root' ? 'All Branches' : label}</span>{type !== 'device' && countNodes(node) > 0 ? <small>{countNodes(node).toLocaleString()}</small> : type === 'device' ? <small>Device</small> : null}</EmaSidebarTreeRow>{children.length && open ? renderTree(children, depth + 1) : null}</div>; })}</div>; }

  const statusColumns: EmaTableColumn<RestrictionStatusRow>[] = [{ key: 'device', header: 'Device', render: (row) => <span><strong className="block text-slate-950">{pick(row, ['ComputerName', 'Object_DeviceID', 'DeviceID', 'deviceName', 'hostname'])}</strong><small className="font-bold text-slate-500">{pick(row, ['Branch', 'Department', 'Object_Full_Name', 'location'], '')}</small></span> }, { key: 'package', header: isWhitelist ? 'Software' : 'Package', render: (row) => getPackageName(row) }, { key: 'status', header: 'Status', render: (row) => { const value = pick(row, ['Status', 'status', 'Result', 'result', 'State', 'state'], 'Unknown'); return <Badge className={badgeClass(value)}>{value}</Badge>; } }, { key: 'updated', header: 'Updated', render: (row) => pick(row, ['UpdatedAt', 'updatedAt', 'LastUpdate', 'lastSeen', 'LastSeen'], '-') }];
  const policyColumns: EmaTableColumn<RestrictionPolicyRow>[] = [{ key: 'policy', header: 'Policy', render: (row) => pick(row, ['PolicyName', 'policyName', 'name', 'policy_id', 'PolicyID'], '-') }, { key: 'target', header: 'Target', render: (row) => pick(row, ['Object_Full_Name', 'targetName', 'Target', 'Branch'], '-') }, { key: 'status', header: 'Status', render: (row) => { const value = pick(row, ['status', 'Status', 'use_policy', 'enabled'], 'Active'); return <Badge className={badgeClass(value)}>{value}</Badge>; } }, { key: 'updated', header: 'Updated', render: (row) => pick(row, ['UpdatedAt', 'updatedAt', 'ModifiedDate', 'created_at'], '-') }];
  const lookupColumns: EmaTableColumn<LookupRow>[] = [{ key: 'state', header: 'Status', width: '7rem', render: (row) => selectedSet.has(getPackageId(row)) ? <Badge className={isWhitelist ? 'border-emerald-200 bg-emerald-50 text-emerald-700' : 'border-rose-200 bg-rose-50 text-rose-700'}>Selected</Badge> : <Badge className="border-slate-200 bg-slate-50 text-slate-500">Available</Badge> }, { key: 'name', header: isWhitelist ? 'Whitelist Software' : 'Restricted Package', render: (row) => <span><strong className="block text-slate-950">{getPackageName(row)}</strong><small className="font-bold text-slate-500">ID: {getPackageId(row) || '-'}</small></span> }, { key: 'company', header: 'Company', render: (row) => pick(row, ['SW_Pkg_Company', 'Company', 'vendor'], 'No company') }, { key: 'action', header: 'Action', align: 'right', render: (row) => { const id = getPackageId(row); const selected = selectedSet.has(id); return <button type="button" onClick={() => togglePackage(id)} className={cn('inline-flex h-9 items-center justify-center gap-2 rounded-xl border px-3 text-xs font-black transition', selected && isWhitelist && 'border-emerald-200 bg-emerald-50 text-emerald-700', selected && !isWhitelist && 'border-rose-200 bg-rose-50 text-rose-700', !selected && 'border-slate-200 bg-white text-slate-700 hover:border-blue-300 hover:bg-blue-50 hover:text-blue-700')}>{selected ? <CheckCircle2 size={14} /> : <Package size={14} />}{selected ? 'Selected' : 'Add'}</button>; } }];

  const sidebar = <EmaSidebarPanel eyebrow="App Control" title="Branch Scope" description="Select branch or device before applying policy." tabs={[{ id: 'branch', label: 'Branch', icon: <FolderOpen size={16} /> }, { id: 'policy', label: 'Policy', icon: <Database size={16} /> }]} activeTab="branch" onTabChange={() => undefined} searchValue={treeSearch} searchPlaceholder="Search branches..." onSearchChange={setTreeSearch}>{loadingTree ? <div className="flex items-center gap-2 rounded-xl border border-blue-100 bg-blue-50 p-4 text-sm font-bold text-blue-700"><Loader2 size={16} className="animate-spin" /> Loading branches...</div> : filteredTree.length ? renderTree(filteredTree) : <div className="rounded-xl border border-slate-200 p-4 text-sm font-bold text-slate-500">No branch scope found.</div>}</EmaSidebarPanel>;

  return <><EmaToastViewport items={toasts} onClose={(id) => setToasts((items) => items.filter((item) => item.id !== id))} /><EmaPageLayout title="Application Restriction" subtitle="Manage application restriction policies." sidebar={sidebar}><div className="space-y-3" data-section="app-restriction"><EmaSection eyebrow="Application Governance" title={moduleConfig.label} description={moduleConfig.helper} action={<div className="inline-flex rounded-xl border border-slate-200 bg-slate-50 p-1">{MODULES.map((item) => { const Icon = item.icon; const active = item.id === activeModule; return <button key={item.id} type="button" onClick={() => setActiveModule(item.id)} className={cn('flex h-10 items-center gap-2 rounded-lg px-4 text-sm font-black transition', active ? 'bg-blue-600 text-white shadow-sm' : 'text-slate-600 hover:bg-white hover:text-slate-950')}><Icon size={16} />{item.shortLabel}</button>; })}</div>}><EmaKpiGrid><EmaKpiCard title="Target" value={selectedTarget?.label || 'All Branches'} note="current scope" icon={<Layers size={16} />} /><EmaKpiCard title={isWhitelist ? 'Allowed Software' : 'Blocked Packages'} value={selectedLookupCount} note={`${lookupRows.length} available`} icon={<Package size={16} />} tone={isWhitelist ? 'emerald' : 'rose'} /><EmaKpiCard title="Status Rows" value={statusRows.length} note="monitor records" icon={<CheckCircle2 size={16} />} tone="violet" /><EmaKpiCard title="Policy Rows" value={policyRows.length} note="registry records" icon={<Ban size={16} />} tone="amber" /></EmaKpiGrid></EmaSection><EmaToolbar left={<div className="inline-flex rounded-xl border border-slate-200 bg-slate-50 p-1">{(Object.keys(TAB_LABELS) as SubTab[]).map((tab) => <button key={tab} type="button" onClick={() => setActiveTab(tab)} className={cn('rounded-lg px-4 py-2 text-sm font-black transition', activeTab === tab ? 'bg-blue-600 text-white shadow-sm' : 'text-slate-500 hover:bg-white hover:text-slate-900')}>{TAB_LABELS[tab]}</button>)}</div>} right={<><EmaButton variant="secondary" onClick={() => void loadPolicyData()}><RefreshCw size={15} className={loadingData ? 'animate-spin' : ''} /> Refresh</EmaButton><EmaButton variant="ghost" onClick={() => { setStatusSearch(''); setPolicySearch(''); setPackageSearch(''); setPackageView('all'); setStartDate(daysAgo(30)); setEndDate(today()); }}><RotateCcw size={15} /> Reset</EmaButton></>} />{activeTab === 'status' ? <div className="space-y-3"><EmaToolbar search={<EmaSearchInput value={statusSearch} onChange={setStatusSearch} placeholder="Search device, package, status..." />} filters={<><EmaFilterField label="Date From"><input type="date" value={startDate} onChange={(event) => setStartDate(event.target.value)} /></EmaFilterField><EmaFilterField label="Date To"><input type="date" value={endDate} onChange={(event) => setEndDate(event.target.value)} /></EmaFilterField><EmaButton variant={includeSub ? 'primary' : 'secondary'} onClick={() => setIncludeSub((value) => !value)}>Include Sub</EmaButton></>} /><EmaTableShell title="Restriction Status" subtitle="Current application restriction status by device."><EmaTable columns={statusColumns} rows={statusPaged.rows} loading={loadingData} emptyText="No restriction status found." getRowKey={(row, index) => `${pick(row, ['id', 'ID', 'Object_DeviceID', 'ComputerName'], 'row')}-${statusPaged.start + index}`} />{filteredStatusRows.length > PAGE_SIZE ? <EmaPagination page={statusPaged.safePage} totalPages={statusPaged.totalPages} totalLabel={`${statusPaged.start + 1}-${Math.min(filteredStatusRows.length, statusPaged.start + PAGE_SIZE)} of ${filteredStatusRows.length}`} onPageChange={setStatusPage} /> : null}</EmaTableShell></div> : null}{activeTab === 'settings' ? <div className="space-y-3"><EmaSection eyebrow="Policy Settings" title="Restriction Rule" description="Update policy behaviour, schedule and package list."><div className="grid gap-3 lg:grid-cols-3"><div className="rounded-xl border border-slate-200 bg-slate-50 p-3"><FieldLabel>Update Interval</FieldLabel><input value={form.updateInterval} onChange={(event) => updateForm('updateInterval', event.target.value)} className="h-10 w-full rounded-xl border border-slate-200 bg-white px-3 text-sm font-bold outline-none" /></div><div className="rounded-xl border border-slate-200 bg-slate-50 p-3"><FieldLabel>{isWhitelist ? 'Process Restrict Type' : 'App Restrict Type'}</FieldLabel><select value={isWhitelist ? form.processRestrictType : form.appRestrictType} onChange={(event) => updateForm(isWhitelist ? 'processRestrictType' : 'appRestrictType', event.target.value)} className="h-10 w-full rounded-xl border border-slate-200 bg-white px-3 text-sm font-bold outline-none">{(isWhitelist ? [{ value: '0', label: 'Disabled' }, { value: '1', label: 'Allow Whitelist' }, { value: '2', label: 'Audit Only' }, { value: '3', label: 'Strict Mode' }] : [{ value: '1', label: 'Block Package' }, { value: '2', label: 'Warn User' }, { value: '3', label: 'Audit Only' }]).map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select></div><div className="rounded-xl border border-slate-200 bg-slate-50 p-3"><FieldLabel>Weekly Policy</FieldLabel><button type="button" onClick={() => updateForm('weeklyPolicy', !form.weeklyPolicy)} className={cn('h-10 w-full rounded-xl border px-4 text-sm font-black transition', form.weeklyPolicy ? 'border-blue-300 bg-blue-50 text-blue-700' : 'border-slate-200 bg-white text-slate-500')}>{form.weeklyPolicy ? 'Enabled' : 'Disabled'}</button></div></div></EmaSection><EmaToolbar left={<div><p className="text-[0.68rem] font-black uppercase tracking-[0.16em] text-slate-500">{isWhitelist ? 'Whitelist Software' : 'Restricted Package'}</p><h3 className="text-base font-extrabold text-slate-950">{selectedLookupCount} selected from {lookupRows.length}</h3></div>} search={<EmaSearchInput value={packageSearch} onChange={setPackageSearch} placeholder={isWhitelist ? 'Search whitelist software...' : 'Search package...'} />} filters={<><EmaButton variant={packageView === 'all' ? 'primary' : 'secondary'} onClick={() => setPackageView('all')}>All {lookupRows.length}</EmaButton><EmaButton variant={packageView === 'selected' ? 'primary' : 'secondary'} onClick={() => setPackageView('selected')}>Selected {selectedLookupCount}</EmaButton><EmaButton variant={packageView === 'available' ? 'primary' : 'secondary'} onClick={() => setPackageView('available')}>Available {availableLookupCount}</EmaButton></>} /><EmaTableShell title={isWhitelist ? 'Whitelist Software' : 'Restricted Package'} subtitle="Compact package picker using the same table layout as the inventory modules."><EmaTable columns={lookupColumns} rows={lookupPaged.rows} loading={loadingData} emptyText="No package found." getRowKey={(row, index) => `${getPackageId(row) || getPackageName(row)}-${lookupPaged.start + index}`} />{filteredLookupRows.length > LOOKUP_PAGE_SIZE ? <EmaPagination page={lookupPaged.safePage} totalPages={lookupPaged.totalPages} totalLabel={`${lookupPaged.start + 1}-${Math.min(filteredLookupRows.length, lookupPaged.start + LOOKUP_PAGE_SIZE)} of ${filteredLookupRows.length}`} onPageChange={setPackagePage} /> : null}</EmaTableShell><EmaSection eyebrow="Schedule" title="Notice and Policy Window" description="Set user notice and weekly schedule."><div className="grid gap-3 lg:grid-cols-2"><div className="rounded-xl border border-slate-200 bg-slate-50 p-3"><FieldLabel>Notice Message</FieldLabel><textarea value={isWhitelist ? form.processNoticeMessage : form.appNoticeMessage} onChange={(event) => updateForm(isWhitelist ? 'processNoticeMessage' : 'appNoticeMessage', event.target.value)} rows={4} className="w-full rounded-xl border border-slate-200 bg-white px-3 py-2 text-sm font-semibold outline-none" placeholder="Message shown to user when policy is applied." /></div><div className="rounded-xl border border-slate-200 bg-slate-50 p-3"><FieldLabel>Policy Days</FieldLabel><div className="flex flex-wrap gap-2">{DAY_OPTIONS.map((day) => { const selected = selectedDays.includes(day); return <button key={day} type="button" onClick={() => toggleDay(day)} className={cn('h-9 rounded-xl border px-3 text-xs font-black transition', selected ? 'border-blue-300 bg-blue-50 text-blue-700' : 'border-slate-200 bg-white text-slate-500')}>{day}</button>; })}</div><div className="mt-3 grid gap-3 sm:grid-cols-2">{[1, 2, 3, 4].map((slot) => <input key={slot} value={(form as any)[`schedule${slot}`]} onChange={(event) => updateForm(`schedule${slot}` as keyof FormState, event.target.value)} placeholder={`Schedule ${slot}`} className="h-10 rounded-xl border border-slate-200 bg-white px-3 text-sm font-bold outline-none" />)}</div></div></div></EmaSection><div className="flex justify-end"><EmaButton variant="primary" onClick={() => void savePolicy()} disabled={saving || loadingData}>{saving ? <Loader2 size={15} className="animate-spin" /> : <Save size={15} />} Save Policy</EmaButton></div></div> : null}{activeTab === 'policyStatus' ? <div className="space-y-3"><EmaToolbar search={<EmaSearchInput value={policySearch} onChange={setPolicySearch} placeholder="Search policy status..." />} /><EmaTableShell title="Policy Status" subtitle="Application restriction policy registry."><EmaTable columns={policyColumns} rows={policyPaged.rows} loading={loadingData} emptyText="No policy status found." getRowKey={(row, index) => `${pick(row, ['id', 'ID', 'policy_id', 'PolicyID'], 'policy')}-${policyPaged.start + index}`} />{filteredPolicyRows.length > PAGE_SIZE ? <EmaPagination page={policyPaged.safePage} totalPages={policyPaged.totalPages} totalLabel={`${policyPaged.start + 1}-${Math.min(filteredPolicyRows.length, policyPaged.start + PAGE_SIZE)} of ${filteredPolicyRows.length}`} onPageChange={setPolicyPage} /> : null}</EmaTableShell></div> : null}</div></EmaPageLayout></>;
}
